// array for tracking compute jobs
const compute_ids = [];


exports.addComputeJob = function(contractAddress, computeId) {

    // debugging / logging
    console.log("STARTING compute job with the following arguments:");
    console.log("Contract Address: " + contractAddress);
    console.log("Compute Job ID: " + computeId);

    // TODO - code to start a compute job
}


exports.killComputeJob = function(contractAddress, computeId) {

    // debugging / logging
    console.log("KILLING compute job with the following arguments:");
    console.log("Contract Address: " + contractAddress);
    console.log("Compute Job ID: " + computeId);

    // TODO - code to kill a compute job
}